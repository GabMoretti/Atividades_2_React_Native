import { Button, Text, TextInput, View, } from "react-native";
import { Container, Input } from "./styles";

function Login() {

    return(
        <Container>
            <Input onChangeText={}></Input>

            <Input></Input>
            <Button title="login"/>
        </Container>
    )
}

export default Login